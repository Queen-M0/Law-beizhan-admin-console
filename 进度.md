成了，构建也通过：`pnpm run build` 成功，只有现有 Sass/Rollup 依赖警告和 chunk size 提示。

1. 修改/新增文件：
   * 新增：`src/api/about.ts`、`src/api/news.ts`、`src/api/organization.ts`、`src/api/upload.ts`
   * 修改：`src/views/about/index.vue`、`src/views/news/index.vue`、`src/views/organizations/index.vue`
   * 修改：`src/components/admin-crud-page.vue`、`src/types/admin.ts`
   * `src/api/admin.ts` 只有末尾换行差异，无业务逻辑变更
2. 路由/菜单：
   * 没有新增侧边栏菜单
   * 复用已有 `/news` 资讯管理、`/organizations` 机构管理、`/about` 关于北展
   * 关于北展页面内部改为 4 个 tabs：栏目内容、发展历程、荣誉资质、媒体报道
3. 三个模块支持：
   * 查询、重置、新增、编辑、删除确认、状态切换、分页、loading、空数据状态
   * 图片路径输入、图片预览、预留上传
   * slug 校验、基础必填校验、排序默认 0、状态默认启用
   * SEO 字段放入折叠区域
4. 等后端联调：
   * `/admin/about/pages`
   * `/admin/about/history`
   * `/admin/about/honors`
   * `/admin/about/media-reports`
   * `/admin/news`
   * `/admin/organizations`
   * `/admin/files/upload`
5. 确认：
   * 没有修改律师管理页面
   * 没有修改专家管理页面
   * 没有修改擅长领域管理页面
   * 没有调整登录、布局、权限、用户信息逻辑
